class CardInfo {
  constructor (name, number, expMonth, expYear, cvv) {
    this.name = name;
    this.number = number;
    this.expMonth = expMonth;
    this.expYear = expYear;
    this.cvv = cvv;
  }
}